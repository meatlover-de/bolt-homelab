---
version: 5
defaults:
  datadir: data
  data_hash: yaml_data
hierarchy:
  - name: "Default"
    globs:
      - "nodes/%{facts.networking.fqdn}.yaml"
      - "modules/*.yaml"
      - "global.yaml"

